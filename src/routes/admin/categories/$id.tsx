import{createFileRoute}from"@tanstack/react-router";import{useQuery}from"@tanstack/react-query";import{supabase}from"@/integrations/supabase/client";import{CategoryForm}from"@/components/admin/CategoryForm";export const Route=createFileRoute("/admin/categories/$id")({component:Edit});function Edit(){const{id}=Route.useParams();const q=useQuery({queryKey:["admin-category",id],queryFn:async()=>{const{data,error}=await supabase.from("categories").select("id,name,slug,parent_id,sort_order").eq("id",id).single();if(error)throw error;return data}});return <><h1 className="mb-6 text-2xl font-black">تعديل تصنيف</h1>{q.data?<CategoryForm category={q.data}/>:<p>جاري التحميل...</p>}</>}
